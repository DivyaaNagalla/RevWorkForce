package com.revworkforce.service;

import com.revworkforce.dao.AuditDAO;

public class AuditService {

    private AuditDAO dao = new AuditDAO();

    public void log(int userId, String action) {
        try {
            dao.logAction(userId, action);
        } catch (Exception e) {
           
        }
    }
}
